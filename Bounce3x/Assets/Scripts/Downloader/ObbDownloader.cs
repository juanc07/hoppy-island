using UnityEngine;
using System.Collections;

public class ObbDownloader : MonoBehaviour {
	
	string main;
	
	// Use this for initialization
	void Start () {
		//main = String.Format("{0}/main.{1}.{2}.obb", expansionFilePath, obb_version, obb_package);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
